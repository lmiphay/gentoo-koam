#!/usr/bin/python

import sys
import os
import koam

print("sys.path=" + str(sys.path))

if len(sys.argv) < 2:
    sys.exit("usage: " + os.path.basename(sys.argv[0]) + " <server0>...<serverN>")
else:
    sys.exit(koam.KoamApplication(sys.argv).run())
